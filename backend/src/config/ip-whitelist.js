/**
 * IP白名单配置
 * 配置允许访问的IP地址
 */

module.exports = {
  // 管理后台IP白名单
  admin: [
    '127.0.0.1',           // 本地
    '::1',                // IPv6本地
    '192.168.0.0/16',     // 内网
    '10.0.0.0/8',         // 私有网络
    '172.16.0.0/12'       // 私有网络
    // 添加特定的管理员IP
    // '123.123.123.123'
  ],

  // API接口IP白名单 (空表示允许所有IP)
  api: [
    // 公开API,允许所有IP访问
    // 如需限制,请添加IP地址
  ],

  // WebSocket连接IP白名单
  websocket: [
    // WebSocket连接IP限制
  ],

  // 敏感操作IP白名单
  sensitive: [
    // 敏感操作(如删除、批量操作)的IP限制
    '127.0.0.1',
    '192.168.0.0/16'
  ],

  // 测试环境IP白名单
  test: [
    '127.0.0.1',
    '192.168.0.0/16'
  ],

  // 生产环境IP白名单
  production: [
    // 生产环境的IP白名单
  ]
};
